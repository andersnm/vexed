{
  "name": "vexed-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "test": "npm run test -ws",
    "start": "npm run start --workspace=vexed-cli --"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
