class Main() {
    public directArray: string[] = ["direct1", "direct2", "direct3"];
    public result: string[] = this.makeArray("test");

    makeArray(s: string): string[] {
        return [s, s + "2", s + "3"];
    }
}
