class Main() {
  public string fileName = (typeof this).scriptPath + "/" + "textfile.txt";
  public string content = io.readTextFile(this.getFileName());
  public string warped = this.testAsyncParam(this.content);

  string testAsyncParam(string asynced) {
    any tmp = io.print(asynced); # Prints "Promise<string>"
    return asynced;
  }

  string getFileName() {
    return this.fileName;
  }
}
