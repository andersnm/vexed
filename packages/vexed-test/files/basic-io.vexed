class Main() {
  public fileName: string = (typeof this).scriptPath + "/" + "textfile.txt";
  public content: string = io.readTextFile(this.getFileName());
  public warped: string = this.testAsyncParam(this.content);

  string testAsyncParam(asynced: string) {
    let tmp: any = io.print(asynced); # Prints "Promise<string>"
    return asynced;
  }

  string getFileName() {
    return this.fileName;
  }
}
